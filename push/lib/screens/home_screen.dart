import 'package:flutter/material.dart';
import 'package:push/widgets/homescreen_widgets.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: null,
      body: HomeScreenWidgets(),
    );
  }
}